// Shared ceiling for the "short" animations required by
// docs/DESIGN_SYSTEM.md's Motion section ("Animations must not block
// interaction for long periods").
export const MAX_SHORT_ANIMATION_DURATION_MS = 600;
